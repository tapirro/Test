<?php

/**
 * Base class that represents a row from the 'usr' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.0-dev on:
 *
 * Wed Mar 31 16:35:32 2010
 *
 * @package    lib.model.om
 */
abstract class BaseUsr extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        UsrPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the pass field.
	 * @var        string
	 */
	protected $pass;

	/**
	 * The value for the login field.
	 * @var        string
	 */
	protected $login;

	/**
	 * The value for the email field.
	 * @var        string
	 */
	protected $email;

	/**
	 * The value for the upic field.
	 * @var        int
	 */
	protected $upic;

	/**
	 * The value for the rank field.
	 * @var        int
	 */
	protected $rank;

	/**
	 * The value for the balance field.
	 * @var        double
	 */
	protected $balance;

	/**
	 * The value for the bot field.
	 * @var        boolean
	 */
	protected $bot;

	/**
	 * The value for the hidden field.
	 * @var        boolean
	 */
	protected $hidden;

	/**
	 * The value for the color field.
	 * @var        string
	 */
	protected $color;

	/**
	 * The value for the date_birth field.
	 * @var        string
	 */
	protected $date_birth;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * @var        SysPic
	 */
	protected $aSysPic;

	/**
	 * @var        array Blog[] Collection to store aggregation of Blog objects.
	 */
	protected $collBlogs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collBlogs.
	 */
	private $lastBlogCriteria = null;

	/**
	 * @var        array BlogComment[] Collection to store aggregation of BlogComment objects.
	 */
	protected $collBlogComments;

	/**
	 * @var        Criteria The criteria used to select the current contents of collBlogComments.
	 */
	private $lastBlogCommentCriteria = null;

	/**
	 * @var        array Permission[] Collection to store aggregation of Permission objects.
	 */
	protected $collPermissions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPermissions.
	 */
	private $lastPermissionCriteria = null;

	/**
	 * @var        array Picture[] Collection to store aggregation of Picture objects.
	 */
	protected $collPicturesRelatedByUserid;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPicturesRelatedByUserid.
	 */
	private $lastPictureRelatedByUseridCriteria = null;

	/**
	 * @var        array Picture[] Collection to store aggregation of Picture objects.
	 */
	protected $collPicturesRelatedByOwner;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPicturesRelatedByOwner.
	 */
	private $lastPictureRelatedByOwnerCriteria = null;

	/**
	 * @var        array PictureComment[] Collection to store aggregation of PictureComment objects.
	 */
	protected $collPictureComments;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPictureComments.
	 */
	private $lastPictureCommentCriteria = null;

	/**
	 * @var        array Test[] Collection to store aggregation of Test objects.
	 */
	protected $collTests;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTests.
	 */
	private $lastTestCriteria = null;

	/**
	 * @var        array Post[] Collection to store aggregation of Post objects.
	 */
	protected $collPosts;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPosts.
	 */
	private $lastPostCriteria = null;

	/**
	 * @var        array Task[] Collection to store aggregation of Task objects.
	 */
	protected $collTasksRelatedByUserid;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTasksRelatedByUserid.
	 */
	private $lastTaskRelatedByUseridCriteria = null;

	/**
	 * @var        array Task[] Collection to store aggregation of Task objects.
	 */
	protected $collTasksRelatedByAuthorid;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTasksRelatedByAuthorid.
	 */
	private $lastTaskRelatedByAuthoridCriteria = null;

	/**
	 * @var        array TaskComment[] Collection to store aggregation of TaskComment objects.
	 */
	protected $collTaskComments;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTaskComments.
	 */
	private $lastTaskCommentCriteria = null;

	/**
	 * @var        array PostComment[] Collection to store aggregation of PostComment objects.
	 */
	protected $collPostComments;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPostComments.
	 */
	private $lastPostCommentCriteria = null;

	/**
	 * @var        array Rank[] Collection to store aggregation of Rank objects.
	 */
	protected $collRanks;

	/**
	 * @var        Criteria The criteria used to select the current contents of collRanks.
	 */
	private $lastRankCriteria = null;

	/**
	 * @var        array Statmsg[] Collection to store aggregation of Statmsg objects.
	 */
	protected $collStatmsgs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStatmsgs.
	 */
	private $lastStatmsgCriteria = null;

	/**
	 * @var        array UsrComment[] Collection to store aggregation of UsrComment objects.
	 */
	protected $collUsrCommentsRelatedByUserid;

	/**
	 * @var        Criteria The criteria used to select the current contents of collUsrCommentsRelatedByUserid.
	 */
	private $lastUsrCommentRelatedByUseridCriteria = null;

	/**
	 * @var        array UsrComment[] Collection to store aggregation of UsrComment objects.
	 */
	protected $collUsrCommentsRelatedByUser2;

	/**
	 * @var        Criteria The criteria used to select the current contents of collUsrCommentsRelatedByUser2.
	 */
	private $lastUsrCommentRelatedByUser2Criteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'UsrPeer';

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [pass] column value.
	 * 
	 * @return     string
	 */
	public function getPass()
	{
		return $this->pass;
	}

	/**
	 * Get the [login] column value.
	 * 
	 * @return     string
	 */
	public function getLogin()
	{
		return $this->login;
	}

	/**
	 * Get the [email] column value.
	 * 
	 * @return     string
	 */
	public function getEmail()
	{
		return $this->email;
	}

	/**
	 * Get the [upic] column value.
	 * 
	 * @return     int
	 */
	public function getUpic()
	{
		return $this->upic;
	}

	/**
	 * Get the [rank] column value.
	 * 
	 * @return     int
	 */
	public function getRank()
	{
		return $this->rank;
	}

	/**
	 * Get the [balance] column value.
	 * 
	 * @return     double
	 */
	public function getBalance()
	{
		return $this->balance;
	}

	/**
	 * Get the [bot] column value.
	 * 
	 * @return     boolean
	 */
	public function getBot()
	{
		return $this->bot;
	}

	/**
	 * Get the [hidden] column value.
	 * 
	 * @return     boolean
	 */
	public function getHidden()
	{
		return $this->hidden;
	}

	/**
	 * Get the [color] column value.
	 * 
	 * @return     string
	 */
	public function getColor()
	{
		return $this->color;
	}

	/**
	 * Get the [optionally formatted] temporal [date_birth] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDateBirth($format = 'Y-m-d H:i:s')
	{
		if ($this->date_birth === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->date_birth);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->date_birth, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->created_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->updated_at);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = UsrPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [pass] column.
	 * 
	 * @param      string $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setPass($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->pass !== $v) {
			$this->pass = $v;
			$this->modifiedColumns[] = UsrPeer::PASS;
		}

		return $this;
	} // setPass()

	/**
	 * Set the value of [login] column.
	 * 
	 * @param      string $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setLogin($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->login !== $v) {
			$this->login = $v;
			$this->modifiedColumns[] = UsrPeer::LOGIN;
		}

		return $this;
	} // setLogin()

	/**
	 * Set the value of [email] column.
	 * 
	 * @param      string $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setEmail($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->email !== $v) {
			$this->email = $v;
			$this->modifiedColumns[] = UsrPeer::EMAIL;
		}

		return $this;
	} // setEmail()

	/**
	 * Set the value of [upic] column.
	 * 
	 * @param      int $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setUpic($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->upic !== $v) {
			$this->upic = $v;
			$this->modifiedColumns[] = UsrPeer::UPIC;
		}

		if ($this->aSysPic !== null && $this->aSysPic->getId() !== $v) {
			$this->aSysPic = null;
		}

		return $this;
	} // setUpic()

	/**
	 * Set the value of [rank] column.
	 * 
	 * @param      int $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setRank($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->rank !== $v) {
			$this->rank = $v;
			$this->modifiedColumns[] = UsrPeer::RANK;
		}

		return $this;
	} // setRank()

	/**
	 * Set the value of [balance] column.
	 * 
	 * @param      double $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setBalance($v)
	{
		if ($v !== null) {
			$v = (double) $v;
		}

		if ($this->balance !== $v) {
			$this->balance = $v;
			$this->modifiedColumns[] = UsrPeer::BALANCE;
		}

		return $this;
	} // setBalance()

	/**
	 * Set the value of [bot] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setBot($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->bot !== $v) {
			$this->bot = $v;
			$this->modifiedColumns[] = UsrPeer::BOT;
		}

		return $this;
	} // setBot()

	/**
	 * Set the value of [hidden] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setHidden($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->hidden !== $v) {
			$this->hidden = $v;
			$this->modifiedColumns[] = UsrPeer::HIDDEN;
		}

		return $this;
	} // setHidden()

	/**
	 * Set the value of [color] column.
	 * 
	 * @param      string $v new value
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setColor($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->color !== $v) {
			$this->color = $v;
			$this->modifiedColumns[] = UsrPeer::COLOR;
		}

		return $this;
	} // setColor()

	/**
	 * Sets the value of [date_birth] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setDateBirth($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->date_birth !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->date_birth !== null && $tmpDt = new DateTime($this->date_birth)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->date_birth = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = UsrPeer::DATE_BIRTH;
			}
		} // if either are not null

		return $this;
	} // setDateBirth()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = UsrPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Usr The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d\\TH:i:sO') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d\\TH:i:sO') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d\\TH:i:sO') : null);
				$this->modifiedColumns[] = UsrPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->pass = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->login = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->email = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->upic = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->rank = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->balance = ($row[$startcol + 6] !== null) ? (double) $row[$startcol + 6] : null;
			$this->bot = ($row[$startcol + 7] !== null) ? (boolean) $row[$startcol + 7] : null;
			$this->hidden = ($row[$startcol + 8] !== null) ? (boolean) $row[$startcol + 8] : null;
			$this->color = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->date_birth = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->created_at = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->updated_at = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 13; // 13 = UsrPeer::NUM_COLUMNS - UsrPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Usr object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aSysPic !== null && $this->upic !== $this->aSysPic->getId()) {
			$this->aSysPic = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(UsrPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = UsrPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aSysPic = null;
			$this->collBlogs = null;
			$this->lastBlogCriteria = null;

			$this->collBlogComments = null;
			$this->lastBlogCommentCriteria = null;

			$this->collPermissions = null;
			$this->lastPermissionCriteria = null;

			$this->collPicturesRelatedByUserid = null;
			$this->lastPictureRelatedByUseridCriteria = null;

			$this->collPicturesRelatedByOwner = null;
			$this->lastPictureRelatedByOwnerCriteria = null;

			$this->collPictureComments = null;
			$this->lastPictureCommentCriteria = null;

			$this->collTests = null;
			$this->lastTestCriteria = null;

			$this->collPosts = null;
			$this->lastPostCriteria = null;

			$this->collTasksRelatedByUserid = null;
			$this->lastTaskRelatedByUseridCriteria = null;

			$this->collTasksRelatedByAuthorid = null;
			$this->lastTaskRelatedByAuthoridCriteria = null;

			$this->collTaskComments = null;
			$this->lastTaskCommentCriteria = null;

			$this->collPostComments = null;
			$this->lastPostCommentCriteria = null;

			$this->collRanks = null;
			$this->lastRankCriteria = null;

			$this->collStatmsgs = null;
			$this->lastStatmsgCriteria = null;

			$this->collUsrCommentsRelatedByUserid = null;
			$this->lastUsrCommentRelatedByUseridCriteria = null;

			$this->collUsrCommentsRelatedByUser2 = null;
			$this->lastUsrCommentRelatedByUser2Criteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(UsrPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseUsr:delete:pre') as $callable)
			{
			  if ($ret = call_user_func($callable, $this, $con))
			  {
			    return;
			  }
			}

			if ($ret) {
				UsrPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseUsr:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(UsrPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseUsr:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    return $affectedRows;
			  }
			}

			// symfony_timestampable behavior
			if ($this->isModified() && !$this->isColumnModified(UsrPeer::UPDATED_AT))
			{
			  $this->setUpdatedAt(time());
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
				// symfony_timestampable behavior
				if (!$this->isColumnModified(UsrPeer::CREATED_AT))
				{
				  $this->setCreatedAt(time());
				}

			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseUsr:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				$con->commit();
				UsrPeer::addInstanceToPool($this);
				return $affectedRows;
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSysPic !== null) {
				if ($this->aSysPic->isModified() || $this->aSysPic->isNew()) {
					$affectedRows += $this->aSysPic->save($con);
				}
				$this->setSysPic($this->aSysPic);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = UsrPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = UsrPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += UsrPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collBlogs !== null) {
				foreach ($this->collBlogs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collBlogComments !== null) {
				foreach ($this->collBlogComments as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPermissions !== null) {
				foreach ($this->collPermissions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPicturesRelatedByUserid !== null) {
				foreach ($this->collPicturesRelatedByUserid as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPicturesRelatedByOwner !== null) {
				foreach ($this->collPicturesRelatedByOwner as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPictureComments !== null) {
				foreach ($this->collPictureComments as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collTests !== null) {
				foreach ($this->collTests as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPosts !== null) {
				foreach ($this->collPosts as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collTasksRelatedByUserid !== null) {
				foreach ($this->collTasksRelatedByUserid as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collTasksRelatedByAuthorid !== null) {
				foreach ($this->collTasksRelatedByAuthorid as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collTaskComments !== null) {
				foreach ($this->collTaskComments as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPostComments !== null) {
				foreach ($this->collPostComments as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collRanks !== null) {
				foreach ($this->collRanks as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStatmsgs !== null) {
				foreach ($this->collStatmsgs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collUsrCommentsRelatedByUserid !== null) {
				foreach ($this->collUsrCommentsRelatedByUserid as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collUsrCommentsRelatedByUser2 !== null) {
				foreach ($this->collUsrCommentsRelatedByUser2 as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSysPic !== null) {
				if (!$this->aSysPic->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSysPic->getValidationFailures());
				}
			}


			if (($retval = UsrPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collBlogs !== null) {
					foreach ($this->collBlogs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collBlogComments !== null) {
					foreach ($this->collBlogComments as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPermissions !== null) {
					foreach ($this->collPermissions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPicturesRelatedByUserid !== null) {
					foreach ($this->collPicturesRelatedByUserid as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPicturesRelatedByOwner !== null) {
					foreach ($this->collPicturesRelatedByOwner as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPictureComments !== null) {
					foreach ($this->collPictureComments as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collTests !== null) {
					foreach ($this->collTests as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPosts !== null) {
					foreach ($this->collPosts as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collTasksRelatedByUserid !== null) {
					foreach ($this->collTasksRelatedByUserid as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collTasksRelatedByAuthorid !== null) {
					foreach ($this->collTasksRelatedByAuthorid as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collTaskComments !== null) {
					foreach ($this->collTaskComments as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPostComments !== null) {
					foreach ($this->collPostComments as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collRanks !== null) {
					foreach ($this->collRanks as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStatmsgs !== null) {
					foreach ($this->collStatmsgs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collUsrCommentsRelatedByUserid !== null) {
					foreach ($this->collUsrCommentsRelatedByUserid as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collUsrCommentsRelatedByUser2 !== null) {
					foreach ($this->collUsrCommentsRelatedByUser2 as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = UsrPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getPass();
				break;
			case 2:
				return $this->getLogin();
				break;
			case 3:
				return $this->getEmail();
				break;
			case 4:
				return $this->getUpic();
				break;
			case 5:
				return $this->getRank();
				break;
			case 6:
				return $this->getBalance();
				break;
			case 7:
				return $this->getBot();
				break;
			case 8:
				return $this->getHidden();
				break;
			case 9:
				return $this->getColor();
				break;
			case 10:
				return $this->getDateBirth();
				break;
			case 11:
				return $this->getCreatedAt();
				break;
			case 12:
				return $this->getUpdatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = UsrPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getPass(),
			$keys[2] => $this->getLogin(),
			$keys[3] => $this->getEmail(),
			$keys[4] => $this->getUpic(),
			$keys[5] => $this->getRank(),
			$keys[6] => $this->getBalance(),
			$keys[7] => $this->getBot(),
			$keys[8] => $this->getHidden(),
			$keys[9] => $this->getColor(),
			$keys[10] => $this->getDateBirth(),
			$keys[11] => $this->getCreatedAt(),
			$keys[12] => $this->getUpdatedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = UsrPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setPass($value);
				break;
			case 2:
				$this->setLogin($value);
				break;
			case 3:
				$this->setEmail($value);
				break;
			case 4:
				$this->setUpic($value);
				break;
			case 5:
				$this->setRank($value);
				break;
			case 6:
				$this->setBalance($value);
				break;
			case 7:
				$this->setBot($value);
				break;
			case 8:
				$this->setHidden($value);
				break;
			case 9:
				$this->setColor($value);
				break;
			case 10:
				$this->setDateBirth($value);
				break;
			case 11:
				$this->setCreatedAt($value);
				break;
			case 12:
				$this->setUpdatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = UsrPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setPass($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setLogin($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setEmail($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setUpic($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setRank($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setBalance($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setBot($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setHidden($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setColor($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setDateBirth($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setCreatedAt($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setUpdatedAt($arr[$keys[12]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(UsrPeer::DATABASE_NAME);

		if ($this->isColumnModified(UsrPeer::ID)) $criteria->add(UsrPeer::ID, $this->id);
		if ($this->isColumnModified(UsrPeer::PASS)) $criteria->add(UsrPeer::PASS, $this->pass);
		if ($this->isColumnModified(UsrPeer::LOGIN)) $criteria->add(UsrPeer::LOGIN, $this->login);
		if ($this->isColumnModified(UsrPeer::EMAIL)) $criteria->add(UsrPeer::EMAIL, $this->email);
		if ($this->isColumnModified(UsrPeer::UPIC)) $criteria->add(UsrPeer::UPIC, $this->upic);
		if ($this->isColumnModified(UsrPeer::RANK)) $criteria->add(UsrPeer::RANK, $this->rank);
		if ($this->isColumnModified(UsrPeer::BALANCE)) $criteria->add(UsrPeer::BALANCE, $this->balance);
		if ($this->isColumnModified(UsrPeer::BOT)) $criteria->add(UsrPeer::BOT, $this->bot);
		if ($this->isColumnModified(UsrPeer::HIDDEN)) $criteria->add(UsrPeer::HIDDEN, $this->hidden);
		if ($this->isColumnModified(UsrPeer::COLOR)) $criteria->add(UsrPeer::COLOR, $this->color);
		if ($this->isColumnModified(UsrPeer::DATE_BIRTH)) $criteria->add(UsrPeer::DATE_BIRTH, $this->date_birth);
		if ($this->isColumnModified(UsrPeer::CREATED_AT)) $criteria->add(UsrPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(UsrPeer::UPDATED_AT)) $criteria->add(UsrPeer::UPDATED_AT, $this->updated_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(UsrPeer::DATABASE_NAME);

		$criteria->add(UsrPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Usr (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setPass($this->pass);

		$copyObj->setLogin($this->login);

		$copyObj->setEmail($this->email);

		$copyObj->setUpic($this->upic);

		$copyObj->setRank($this->rank);

		$copyObj->setBalance($this->balance);

		$copyObj->setBot($this->bot);

		$copyObj->setHidden($this->hidden);

		$copyObj->setColor($this->color);

		$copyObj->setDateBirth($this->date_birth);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getBlogs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addBlog($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getBlogComments() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addBlogComment($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPermissions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPermission($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPicturesRelatedByUserid() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPictureRelatedByUserid($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPicturesRelatedByOwner() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPictureRelatedByOwner($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPictureComments() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPictureComment($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getTests() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTest($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPosts() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPost($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getTasksRelatedByUserid() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTaskRelatedByUserid($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getTasksRelatedByAuthorid() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTaskRelatedByAuthorid($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getTaskComments() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTaskComment($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPostComments() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPostComment($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getRanks() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addRank($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStatmsgs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStatmsg($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getUsrCommentsRelatedByUserid() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addUsrCommentRelatedByUserid($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getUsrCommentsRelatedByUser2() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addUsrCommentRelatedByUser2($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Usr Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     UsrPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new UsrPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a SysPic object.
	 *
	 * @param      SysPic $v
	 * @return     Usr The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSysPic(SysPic $v = null)
	{
		if ($v === null) {
			$this->setUpic(NULL);
		} else {
			$this->setUpic($v->getId());
		}

		$this->aSysPic = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SysPic object, it will not be re-added.
		if ($v !== null) {
			$v->addUsr($this);
		}

		return $this;
	}


	/**
	 * Get the associated SysPic object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SysPic The associated SysPic object.
	 * @throws     PropelException
	 */
	public function getSysPic(PropelPDO $con = null)
	{
		if ($this->aSysPic === null && ($this->upic !== null)) {
			$this->aSysPic = SysPicPeer::retrieveByPk($this->upic);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSysPic->addUsrs($this);
			 */
		}
		return $this->aSysPic;
	}

	/**
	 * Clears out the collBlogs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addBlogs()
	 */
	public function clearBlogs()
	{
		$this->collBlogs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collBlogs collection (array).
	 *
	 * By default this just sets the collBlogs collection to an empty array (like clearcollBlogs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initBlogs()
	{
		$this->collBlogs = array();
	}

	/**
	 * Gets an array of Blog objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related Blogs from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Blog[]
	 * @throws     PropelException
	 */
	public function getBlogs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collBlogs === null) {
			if ($this->isNew()) {
			   $this->collBlogs = array();
			} else {

				$criteria->add(BlogPeer::USERID, $this->id);

				BlogPeer::addSelectColumns($criteria);
				$this->collBlogs = BlogPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(BlogPeer::USERID, $this->id);

				BlogPeer::addSelectColumns($criteria);
				if (!isset($this->lastBlogCriteria) || !$this->lastBlogCriteria->equals($criteria)) {
					$this->collBlogs = BlogPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastBlogCriteria = $criteria;
		return $this->collBlogs;
	}

	/**
	 * Returns the number of related Blog objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Blog objects.
	 * @throws     PropelException
	 */
	public function countBlogs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collBlogs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(BlogPeer::USERID, $this->id);

				$count = BlogPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(BlogPeer::USERID, $this->id);

				if (!isset($this->lastBlogCriteria) || !$this->lastBlogCriteria->equals($criteria)) {
					$count = BlogPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collBlogs);
				}
			} else {
				$count = count($this->collBlogs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Blog object to this object
	 * through the Blog foreign key attribute.
	 *
	 * @param      Blog $l Blog
	 * @return     void
	 * @throws     PropelException
	 */
	public function addBlog(Blog $l)
	{
		if ($this->collBlogs === null) {
			$this->initBlogs();
		}
		if (!in_array($l, $this->collBlogs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collBlogs, $l);
			$l->setUsr($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related Blogs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getBlogsJoinSysPic($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collBlogs === null) {
			if ($this->isNew()) {
				$this->collBlogs = array();
			} else {

				$criteria->add(BlogPeer::USERID, $this->id);

				$this->collBlogs = BlogPeer::doSelectJoinSysPic($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(BlogPeer::USERID, $this->id);

			if (!isset($this->lastBlogCriteria) || !$this->lastBlogCriteria->equals($criteria)) {
				$this->collBlogs = BlogPeer::doSelectJoinSysPic($criteria, $con, $join_behavior);
			}
		}
		$this->lastBlogCriteria = $criteria;

		return $this->collBlogs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related Blogs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getBlogsJoinMode($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collBlogs === null) {
			if ($this->isNew()) {
				$this->collBlogs = array();
			} else {

				$criteria->add(BlogPeer::USERID, $this->id);

				$this->collBlogs = BlogPeer::doSelectJoinMode($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(BlogPeer::USERID, $this->id);

			if (!isset($this->lastBlogCriteria) || !$this->lastBlogCriteria->equals($criteria)) {
				$this->collBlogs = BlogPeer::doSelectJoinMode($criteria, $con, $join_behavior);
			}
		}
		$this->lastBlogCriteria = $criteria;

		return $this->collBlogs;
	}

	/**
	 * Clears out the collBlogComments collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addBlogComments()
	 */
	public function clearBlogComments()
	{
		$this->collBlogComments = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collBlogComments collection (array).
	 *
	 * By default this just sets the collBlogComments collection to an empty array (like clearcollBlogComments());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initBlogComments()
	{
		$this->collBlogComments = array();
	}

	/**
	 * Gets an array of BlogComment objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related BlogComments from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array BlogComment[]
	 * @throws     PropelException
	 */
	public function getBlogComments($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collBlogComments === null) {
			if ($this->isNew()) {
			   $this->collBlogComments = array();
			} else {

				$criteria->add(BlogCommentPeer::USERID, $this->id);

				BlogCommentPeer::addSelectColumns($criteria);
				$this->collBlogComments = BlogCommentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(BlogCommentPeer::USERID, $this->id);

				BlogCommentPeer::addSelectColumns($criteria);
				if (!isset($this->lastBlogCommentCriteria) || !$this->lastBlogCommentCriteria->equals($criteria)) {
					$this->collBlogComments = BlogCommentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastBlogCommentCriteria = $criteria;
		return $this->collBlogComments;
	}

	/**
	 * Returns the number of related BlogComment objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related BlogComment objects.
	 * @throws     PropelException
	 */
	public function countBlogComments(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collBlogComments === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(BlogCommentPeer::USERID, $this->id);

				$count = BlogCommentPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(BlogCommentPeer::USERID, $this->id);

				if (!isset($this->lastBlogCommentCriteria) || !$this->lastBlogCommentCriteria->equals($criteria)) {
					$count = BlogCommentPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collBlogComments);
				}
			} else {
				$count = count($this->collBlogComments);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a BlogComment object to this object
	 * through the BlogComment foreign key attribute.
	 *
	 * @param      BlogComment $l BlogComment
	 * @return     void
	 * @throws     PropelException
	 */
	public function addBlogComment(BlogComment $l)
	{
		if ($this->collBlogComments === null) {
			$this->initBlogComments();
		}
		if (!in_array($l, $this->collBlogComments, true)) { // only add it if the **same** object is not already associated
			array_push($this->collBlogComments, $l);
			$l->setUsr($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related BlogComments from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getBlogCommentsJoinBlog($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collBlogComments === null) {
			if ($this->isNew()) {
				$this->collBlogComments = array();
			} else {

				$criteria->add(BlogCommentPeer::USERID, $this->id);

				$this->collBlogComments = BlogCommentPeer::doSelectJoinBlog($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(BlogCommentPeer::USERID, $this->id);

			if (!isset($this->lastBlogCommentCriteria) || !$this->lastBlogCommentCriteria->equals($criteria)) {
				$this->collBlogComments = BlogCommentPeer::doSelectJoinBlog($criteria, $con, $join_behavior);
			}
		}
		$this->lastBlogCommentCriteria = $criteria;

		return $this->collBlogComments;
	}

	/**
	 * Clears out the collPermissions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPermissions()
	 */
	public function clearPermissions()
	{
		$this->collPermissions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPermissions collection (array).
	 *
	 * By default this just sets the collPermissions collection to an empty array (like clearcollPermissions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPermissions()
	{
		$this->collPermissions = array();
	}

	/**
	 * Gets an array of Permission objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related Permissions from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Permission[]
	 * @throws     PropelException
	 */
	public function getPermissions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPermissions === null) {
			if ($this->isNew()) {
			   $this->collPermissions = array();
			} else {

				$criteria->add(PermissionPeer::USERID, $this->id);

				PermissionPeer::addSelectColumns($criteria);
				$this->collPermissions = PermissionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PermissionPeer::USERID, $this->id);

				PermissionPeer::addSelectColumns($criteria);
				if (!isset($this->lastPermissionCriteria) || !$this->lastPermissionCriteria->equals($criteria)) {
					$this->collPermissions = PermissionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPermissionCriteria = $criteria;
		return $this->collPermissions;
	}

	/**
	 * Returns the number of related Permission objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Permission objects.
	 * @throws     PropelException
	 */
	public function countPermissions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPermissions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PermissionPeer::USERID, $this->id);

				$count = PermissionPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PermissionPeer::USERID, $this->id);

				if (!isset($this->lastPermissionCriteria) || !$this->lastPermissionCriteria->equals($criteria)) {
					$count = PermissionPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPermissions);
				}
			} else {
				$count = count($this->collPermissions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Permission object to this object
	 * through the Permission foreign key attribute.
	 *
	 * @param      Permission $l Permission
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPermission(Permission $l)
	{
		if ($this->collPermissions === null) {
			$this->initPermissions();
		}
		if (!in_array($l, $this->collPermissions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPermissions, $l);
			$l->setUsr($this);
		}
	}

	/**
	 * Clears out the collPicturesRelatedByUserid collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPicturesRelatedByUserid()
	 */
	public function clearPicturesRelatedByUserid()
	{
		$this->collPicturesRelatedByUserid = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPicturesRelatedByUserid collection (array).
	 *
	 * By default this just sets the collPicturesRelatedByUserid collection to an empty array (like clearcollPicturesRelatedByUserid());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPicturesRelatedByUserid()
	{
		$this->collPicturesRelatedByUserid = array();
	}

	/**
	 * Gets an array of Picture objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related PicturesRelatedByUserid from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Picture[]
	 * @throws     PropelException
	 */
	public function getPicturesRelatedByUserid($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPicturesRelatedByUserid === null) {
			if ($this->isNew()) {
			   $this->collPicturesRelatedByUserid = array();
			} else {

				$criteria->add(PicturePeer::USERID, $this->id);

				PicturePeer::addSelectColumns($criteria);
				$this->collPicturesRelatedByUserid = PicturePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PicturePeer::USERID, $this->id);

				PicturePeer::addSelectColumns($criteria);
				if (!isset($this->lastPictureRelatedByUseridCriteria) || !$this->lastPictureRelatedByUseridCriteria->equals($criteria)) {
					$this->collPicturesRelatedByUserid = PicturePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPictureRelatedByUseridCriteria = $criteria;
		return $this->collPicturesRelatedByUserid;
	}

	/**
	 * Returns the number of related Picture objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Picture objects.
	 * @throws     PropelException
	 */
	public function countPicturesRelatedByUserid(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPicturesRelatedByUserid === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PicturePeer::USERID, $this->id);

				$count = PicturePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PicturePeer::USERID, $this->id);

				if (!isset($this->lastPictureRelatedByUseridCriteria) || !$this->lastPictureRelatedByUseridCriteria->equals($criteria)) {
					$count = PicturePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPicturesRelatedByUserid);
				}
			} else {
				$count = count($this->collPicturesRelatedByUserid);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Picture object to this object
	 * through the Picture foreign key attribute.
	 *
	 * @param      Picture $l Picture
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPictureRelatedByUserid(Picture $l)
	{
		if ($this->collPicturesRelatedByUserid === null) {
			$this->initPicturesRelatedByUserid();
		}
		if (!in_array($l, $this->collPicturesRelatedByUserid, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPicturesRelatedByUserid, $l);
			$l->setUsrRelatedByUserid($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related PicturesRelatedByUserid from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getPicturesRelatedByUseridJoinMode($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPicturesRelatedByUserid === null) {
			if ($this->isNew()) {
				$this->collPicturesRelatedByUserid = array();
			} else {

				$criteria->add(PicturePeer::USERID, $this->id);

				$this->collPicturesRelatedByUserid = PicturePeer::doSelectJoinMode($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PicturePeer::USERID, $this->id);

			if (!isset($this->lastPictureRelatedByUseridCriteria) || !$this->lastPictureRelatedByUseridCriteria->equals($criteria)) {
				$this->collPicturesRelatedByUserid = PicturePeer::doSelectJoinMode($criteria, $con, $join_behavior);
			}
		}
		$this->lastPictureRelatedByUseridCriteria = $criteria;

		return $this->collPicturesRelatedByUserid;
	}

	/**
	 * Clears out the collPicturesRelatedByOwner collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPicturesRelatedByOwner()
	 */
	public function clearPicturesRelatedByOwner()
	{
		$this->collPicturesRelatedByOwner = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPicturesRelatedByOwner collection (array).
	 *
	 * By default this just sets the collPicturesRelatedByOwner collection to an empty array (like clearcollPicturesRelatedByOwner());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPicturesRelatedByOwner()
	{
		$this->collPicturesRelatedByOwner = array();
	}

	/**
	 * Gets an array of Picture objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related PicturesRelatedByOwner from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Picture[]
	 * @throws     PropelException
	 */
	public function getPicturesRelatedByOwner($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPicturesRelatedByOwner === null) {
			if ($this->isNew()) {
			   $this->collPicturesRelatedByOwner = array();
			} else {

				$criteria->add(PicturePeer::OWNER, $this->id);

				PicturePeer::addSelectColumns($criteria);
				$this->collPicturesRelatedByOwner = PicturePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PicturePeer::OWNER, $this->id);

				PicturePeer::addSelectColumns($criteria);
				if (!isset($this->lastPictureRelatedByOwnerCriteria) || !$this->lastPictureRelatedByOwnerCriteria->equals($criteria)) {
					$this->collPicturesRelatedByOwner = PicturePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPictureRelatedByOwnerCriteria = $criteria;
		return $this->collPicturesRelatedByOwner;
	}

	/**
	 * Returns the number of related Picture objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Picture objects.
	 * @throws     PropelException
	 */
	public function countPicturesRelatedByOwner(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPicturesRelatedByOwner === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PicturePeer::OWNER, $this->id);

				$count = PicturePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PicturePeer::OWNER, $this->id);

				if (!isset($this->lastPictureRelatedByOwnerCriteria) || !$this->lastPictureRelatedByOwnerCriteria->equals($criteria)) {
					$count = PicturePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPicturesRelatedByOwner);
				}
			} else {
				$count = count($this->collPicturesRelatedByOwner);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Picture object to this object
	 * through the Picture foreign key attribute.
	 *
	 * @param      Picture $l Picture
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPictureRelatedByOwner(Picture $l)
	{
		if ($this->collPicturesRelatedByOwner === null) {
			$this->initPicturesRelatedByOwner();
		}
		if (!in_array($l, $this->collPicturesRelatedByOwner, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPicturesRelatedByOwner, $l);
			$l->setUsrRelatedByOwner($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related PicturesRelatedByOwner from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getPicturesRelatedByOwnerJoinMode($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPicturesRelatedByOwner === null) {
			if ($this->isNew()) {
				$this->collPicturesRelatedByOwner = array();
			} else {

				$criteria->add(PicturePeer::OWNER, $this->id);

				$this->collPicturesRelatedByOwner = PicturePeer::doSelectJoinMode($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PicturePeer::OWNER, $this->id);

			if (!isset($this->lastPictureRelatedByOwnerCriteria) || !$this->lastPictureRelatedByOwnerCriteria->equals($criteria)) {
				$this->collPicturesRelatedByOwner = PicturePeer::doSelectJoinMode($criteria, $con, $join_behavior);
			}
		}
		$this->lastPictureRelatedByOwnerCriteria = $criteria;

		return $this->collPicturesRelatedByOwner;
	}

	/**
	 * Clears out the collPictureComments collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPictureComments()
	 */
	public function clearPictureComments()
	{
		$this->collPictureComments = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPictureComments collection (array).
	 *
	 * By default this just sets the collPictureComments collection to an empty array (like clearcollPictureComments());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPictureComments()
	{
		$this->collPictureComments = array();
	}

	/**
	 * Gets an array of PictureComment objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related PictureComments from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array PictureComment[]
	 * @throws     PropelException
	 */
	public function getPictureComments($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPictureComments === null) {
			if ($this->isNew()) {
			   $this->collPictureComments = array();
			} else {

				$criteria->add(PictureCommentPeer::USERID, $this->id);

				PictureCommentPeer::addSelectColumns($criteria);
				$this->collPictureComments = PictureCommentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PictureCommentPeer::USERID, $this->id);

				PictureCommentPeer::addSelectColumns($criteria);
				if (!isset($this->lastPictureCommentCriteria) || !$this->lastPictureCommentCriteria->equals($criteria)) {
					$this->collPictureComments = PictureCommentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPictureCommentCriteria = $criteria;
		return $this->collPictureComments;
	}

	/**
	 * Returns the number of related PictureComment objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related PictureComment objects.
	 * @throws     PropelException
	 */
	public function countPictureComments(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPictureComments === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PictureCommentPeer::USERID, $this->id);

				$count = PictureCommentPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PictureCommentPeer::USERID, $this->id);

				if (!isset($this->lastPictureCommentCriteria) || !$this->lastPictureCommentCriteria->equals($criteria)) {
					$count = PictureCommentPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPictureComments);
				}
			} else {
				$count = count($this->collPictureComments);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a PictureComment object to this object
	 * through the PictureComment foreign key attribute.
	 *
	 * @param      PictureComment $l PictureComment
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPictureComment(PictureComment $l)
	{
		if ($this->collPictureComments === null) {
			$this->initPictureComments();
		}
		if (!in_array($l, $this->collPictureComments, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPictureComments, $l);
			$l->setUsr($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related PictureComments from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getPictureCommentsJoinPicture($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPictureComments === null) {
			if ($this->isNew()) {
				$this->collPictureComments = array();
			} else {

				$criteria->add(PictureCommentPeer::USERID, $this->id);

				$this->collPictureComments = PictureCommentPeer::doSelectJoinPicture($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PictureCommentPeer::USERID, $this->id);

			if (!isset($this->lastPictureCommentCriteria) || !$this->lastPictureCommentCriteria->equals($criteria)) {
				$this->collPictureComments = PictureCommentPeer::doSelectJoinPicture($criteria, $con, $join_behavior);
			}
		}
		$this->lastPictureCommentCriteria = $criteria;

		return $this->collPictureComments;
	}

	/**
	 * Clears out the collTests collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTests()
	 */
	public function clearTests()
	{
		$this->collTests = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTests collection (array).
	 *
	 * By default this just sets the collTests collection to an empty array (like clearcollTests());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTests()
	{
		$this->collTests = array();
	}

	/**
	 * Gets an array of Test objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related Tests from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Test[]
	 * @throws     PropelException
	 */
	public function getTests($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTests === null) {
			if ($this->isNew()) {
			   $this->collTests = array();
			} else {

				$criteria->add(TestPeer::USERID, $this->id);

				TestPeer::addSelectColumns($criteria);
				$this->collTests = TestPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TestPeer::USERID, $this->id);

				TestPeer::addSelectColumns($criteria);
				if (!isset($this->lastTestCriteria) || !$this->lastTestCriteria->equals($criteria)) {
					$this->collTests = TestPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTestCriteria = $criteria;
		return $this->collTests;
	}

	/**
	 * Returns the number of related Test objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Test objects.
	 * @throws     PropelException
	 */
	public function countTests(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTests === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TestPeer::USERID, $this->id);

				$count = TestPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TestPeer::USERID, $this->id);

				if (!isset($this->lastTestCriteria) || !$this->lastTestCriteria->equals($criteria)) {
					$count = TestPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTests);
				}
			} else {
				$count = count($this->collTests);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Test object to this object
	 * through the Test foreign key attribute.
	 *
	 * @param      Test $l Test
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTest(Test $l)
	{
		if ($this->collTests === null) {
			$this->initTests();
		}
		if (!in_array($l, $this->collTests, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTests, $l);
			$l->setUsr($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related Tests from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getTestsJoinPicture($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTests === null) {
			if ($this->isNew()) {
				$this->collTests = array();
			} else {

				$criteria->add(TestPeer::USERID, $this->id);

				$this->collTests = TestPeer::doSelectJoinPicture($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TestPeer::USERID, $this->id);

			if (!isset($this->lastTestCriteria) || !$this->lastTestCriteria->equals($criteria)) {
				$this->collTests = TestPeer::doSelectJoinPicture($criteria, $con, $join_behavior);
			}
		}
		$this->lastTestCriteria = $criteria;

		return $this->collTests;
	}

	/**
	 * Clears out the collPosts collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPosts()
	 */
	public function clearPosts()
	{
		$this->collPosts = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPosts collection (array).
	 *
	 * By default this just sets the collPosts collection to an empty array (like clearcollPosts());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPosts()
	{
		$this->collPosts = array();
	}

	/**
	 * Gets an array of Post objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related Posts from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Post[]
	 * @throws     PropelException
	 */
	public function getPosts($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPosts === null) {
			if ($this->isNew()) {
			   $this->collPosts = array();
			} else {

				$criteria->add(PostPeer::USERID, $this->id);

				PostPeer::addSelectColumns($criteria);
				$this->collPosts = PostPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PostPeer::USERID, $this->id);

				PostPeer::addSelectColumns($criteria);
				if (!isset($this->lastPostCriteria) || !$this->lastPostCriteria->equals($criteria)) {
					$this->collPosts = PostPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPostCriteria = $criteria;
		return $this->collPosts;
	}

	/**
	 * Returns the number of related Post objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Post objects.
	 * @throws     PropelException
	 */
	public function countPosts(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPosts === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PostPeer::USERID, $this->id);

				$count = PostPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PostPeer::USERID, $this->id);

				if (!isset($this->lastPostCriteria) || !$this->lastPostCriteria->equals($criteria)) {
					$count = PostPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPosts);
				}
			} else {
				$count = count($this->collPosts);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Post object to this object
	 * through the Post foreign key attribute.
	 *
	 * @param      Post $l Post
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPost(Post $l)
	{
		if ($this->collPosts === null) {
			$this->initPosts();
		}
		if (!in_array($l, $this->collPosts, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPosts, $l);
			$l->setUsr($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related Posts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getPostsJoinBlog($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPosts === null) {
			if ($this->isNew()) {
				$this->collPosts = array();
			} else {

				$criteria->add(PostPeer::USERID, $this->id);

				$this->collPosts = PostPeer::doSelectJoinBlog($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PostPeer::USERID, $this->id);

			if (!isset($this->lastPostCriteria) || !$this->lastPostCriteria->equals($criteria)) {
				$this->collPosts = PostPeer::doSelectJoinBlog($criteria, $con, $join_behavior);
			}
		}
		$this->lastPostCriteria = $criteria;

		return $this->collPosts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related Posts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getPostsJoinMode($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPosts === null) {
			if ($this->isNew()) {
				$this->collPosts = array();
			} else {

				$criteria->add(PostPeer::USERID, $this->id);

				$this->collPosts = PostPeer::doSelectJoinMode($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PostPeer::USERID, $this->id);

			if (!isset($this->lastPostCriteria) || !$this->lastPostCriteria->equals($criteria)) {
				$this->collPosts = PostPeer::doSelectJoinMode($criteria, $con, $join_behavior);
			}
		}
		$this->lastPostCriteria = $criteria;

		return $this->collPosts;
	}

	/**
	 * Clears out the collTasksRelatedByUserid collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTasksRelatedByUserid()
	 */
	public function clearTasksRelatedByUserid()
	{
		$this->collTasksRelatedByUserid = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTasksRelatedByUserid collection (array).
	 *
	 * By default this just sets the collTasksRelatedByUserid collection to an empty array (like clearcollTasksRelatedByUserid());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTasksRelatedByUserid()
	{
		$this->collTasksRelatedByUserid = array();
	}

	/**
	 * Gets an array of Task objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related TasksRelatedByUserid from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Task[]
	 * @throws     PropelException
	 */
	public function getTasksRelatedByUserid($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTasksRelatedByUserid === null) {
			if ($this->isNew()) {
			   $this->collTasksRelatedByUserid = array();
			} else {

				$criteria->add(TaskPeer::USERID, $this->id);

				TaskPeer::addSelectColumns($criteria);
				$this->collTasksRelatedByUserid = TaskPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TaskPeer::USERID, $this->id);

				TaskPeer::addSelectColumns($criteria);
				if (!isset($this->lastTaskRelatedByUseridCriteria) || !$this->lastTaskRelatedByUseridCriteria->equals($criteria)) {
					$this->collTasksRelatedByUserid = TaskPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTaskRelatedByUseridCriteria = $criteria;
		return $this->collTasksRelatedByUserid;
	}

	/**
	 * Returns the number of related Task objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Task objects.
	 * @throws     PropelException
	 */
	public function countTasksRelatedByUserid(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTasksRelatedByUserid === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TaskPeer::USERID, $this->id);

				$count = TaskPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TaskPeer::USERID, $this->id);

				if (!isset($this->lastTaskRelatedByUseridCriteria) || !$this->lastTaskRelatedByUseridCriteria->equals($criteria)) {
					$count = TaskPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTasksRelatedByUserid);
				}
			} else {
				$count = count($this->collTasksRelatedByUserid);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Task object to this object
	 * through the Task foreign key attribute.
	 *
	 * @param      Task $l Task
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTaskRelatedByUserid(Task $l)
	{
		if ($this->collTasksRelatedByUserid === null) {
			$this->initTasksRelatedByUserid();
		}
		if (!in_array($l, $this->collTasksRelatedByUserid, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTasksRelatedByUserid, $l);
			$l->setUsrRelatedByUserid($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related TasksRelatedByUserid from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getTasksRelatedByUseridJoinStat($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTasksRelatedByUserid === null) {
			if ($this->isNew()) {
				$this->collTasksRelatedByUserid = array();
			} else {

				$criteria->add(TaskPeer::USERID, $this->id);

				$this->collTasksRelatedByUserid = TaskPeer::doSelectJoinStat($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TaskPeer::USERID, $this->id);

			if (!isset($this->lastTaskRelatedByUseridCriteria) || !$this->lastTaskRelatedByUseridCriteria->equals($criteria)) {
				$this->collTasksRelatedByUserid = TaskPeer::doSelectJoinStat($criteria, $con, $join_behavior);
			}
		}
		$this->lastTaskRelatedByUseridCriteria = $criteria;

		return $this->collTasksRelatedByUserid;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related TasksRelatedByUserid from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getTasksRelatedByUseridJoinUrgency($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTasksRelatedByUserid === null) {
			if ($this->isNew()) {
				$this->collTasksRelatedByUserid = array();
			} else {

				$criteria->add(TaskPeer::USERID, $this->id);

				$this->collTasksRelatedByUserid = TaskPeer::doSelectJoinUrgency($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TaskPeer::USERID, $this->id);

			if (!isset($this->lastTaskRelatedByUseridCriteria) || !$this->lastTaskRelatedByUseridCriteria->equals($criteria)) {
				$this->collTasksRelatedByUserid = TaskPeer::doSelectJoinUrgency($criteria, $con, $join_behavior);
			}
		}
		$this->lastTaskRelatedByUseridCriteria = $criteria;

		return $this->collTasksRelatedByUserid;
	}

	/**
	 * Clears out the collTasksRelatedByAuthorid collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTasksRelatedByAuthorid()
	 */
	public function clearTasksRelatedByAuthorid()
	{
		$this->collTasksRelatedByAuthorid = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTasksRelatedByAuthorid collection (array).
	 *
	 * By default this just sets the collTasksRelatedByAuthorid collection to an empty array (like clearcollTasksRelatedByAuthorid());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTasksRelatedByAuthorid()
	{
		$this->collTasksRelatedByAuthorid = array();
	}

	/**
	 * Gets an array of Task objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related TasksRelatedByAuthorid from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Task[]
	 * @throws     PropelException
	 */
	public function getTasksRelatedByAuthorid($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTasksRelatedByAuthorid === null) {
			if ($this->isNew()) {
			   $this->collTasksRelatedByAuthorid = array();
			} else {

				$criteria->add(TaskPeer::AUTHORID, $this->id);

				TaskPeer::addSelectColumns($criteria);
				$this->collTasksRelatedByAuthorid = TaskPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TaskPeer::AUTHORID, $this->id);

				TaskPeer::addSelectColumns($criteria);
				if (!isset($this->lastTaskRelatedByAuthoridCriteria) || !$this->lastTaskRelatedByAuthoridCriteria->equals($criteria)) {
					$this->collTasksRelatedByAuthorid = TaskPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTaskRelatedByAuthoridCriteria = $criteria;
		return $this->collTasksRelatedByAuthorid;
	}

	/**
	 * Returns the number of related Task objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Task objects.
	 * @throws     PropelException
	 */
	public function countTasksRelatedByAuthorid(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTasksRelatedByAuthorid === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TaskPeer::AUTHORID, $this->id);

				$count = TaskPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TaskPeer::AUTHORID, $this->id);

				if (!isset($this->lastTaskRelatedByAuthoridCriteria) || !$this->lastTaskRelatedByAuthoridCriteria->equals($criteria)) {
					$count = TaskPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTasksRelatedByAuthorid);
				}
			} else {
				$count = count($this->collTasksRelatedByAuthorid);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Task object to this object
	 * through the Task foreign key attribute.
	 *
	 * @param      Task $l Task
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTaskRelatedByAuthorid(Task $l)
	{
		if ($this->collTasksRelatedByAuthorid === null) {
			$this->initTasksRelatedByAuthorid();
		}
		if (!in_array($l, $this->collTasksRelatedByAuthorid, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTasksRelatedByAuthorid, $l);
			$l->setUsrRelatedByAuthorid($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related TasksRelatedByAuthorid from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getTasksRelatedByAuthoridJoinStat($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTasksRelatedByAuthorid === null) {
			if ($this->isNew()) {
				$this->collTasksRelatedByAuthorid = array();
			} else {

				$criteria->add(TaskPeer::AUTHORID, $this->id);

				$this->collTasksRelatedByAuthorid = TaskPeer::doSelectJoinStat($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TaskPeer::AUTHORID, $this->id);

			if (!isset($this->lastTaskRelatedByAuthoridCriteria) || !$this->lastTaskRelatedByAuthoridCriteria->equals($criteria)) {
				$this->collTasksRelatedByAuthorid = TaskPeer::doSelectJoinStat($criteria, $con, $join_behavior);
			}
		}
		$this->lastTaskRelatedByAuthoridCriteria = $criteria;

		return $this->collTasksRelatedByAuthorid;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related TasksRelatedByAuthorid from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getTasksRelatedByAuthoridJoinUrgency($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTasksRelatedByAuthorid === null) {
			if ($this->isNew()) {
				$this->collTasksRelatedByAuthorid = array();
			} else {

				$criteria->add(TaskPeer::AUTHORID, $this->id);

				$this->collTasksRelatedByAuthorid = TaskPeer::doSelectJoinUrgency($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TaskPeer::AUTHORID, $this->id);

			if (!isset($this->lastTaskRelatedByAuthoridCriteria) || !$this->lastTaskRelatedByAuthoridCriteria->equals($criteria)) {
				$this->collTasksRelatedByAuthorid = TaskPeer::doSelectJoinUrgency($criteria, $con, $join_behavior);
			}
		}
		$this->lastTaskRelatedByAuthoridCriteria = $criteria;

		return $this->collTasksRelatedByAuthorid;
	}

	/**
	 * Clears out the collTaskComments collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTaskComments()
	 */
	public function clearTaskComments()
	{
		$this->collTaskComments = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTaskComments collection (array).
	 *
	 * By default this just sets the collTaskComments collection to an empty array (like clearcollTaskComments());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTaskComments()
	{
		$this->collTaskComments = array();
	}

	/**
	 * Gets an array of TaskComment objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related TaskComments from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array TaskComment[]
	 * @throws     PropelException
	 */
	public function getTaskComments($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTaskComments === null) {
			if ($this->isNew()) {
			   $this->collTaskComments = array();
			} else {

				$criteria->add(TaskCommentPeer::USERID, $this->id);

				TaskCommentPeer::addSelectColumns($criteria);
				$this->collTaskComments = TaskCommentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TaskCommentPeer::USERID, $this->id);

				TaskCommentPeer::addSelectColumns($criteria);
				if (!isset($this->lastTaskCommentCriteria) || !$this->lastTaskCommentCriteria->equals($criteria)) {
					$this->collTaskComments = TaskCommentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTaskCommentCriteria = $criteria;
		return $this->collTaskComments;
	}

	/**
	 * Returns the number of related TaskComment objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related TaskComment objects.
	 * @throws     PropelException
	 */
	public function countTaskComments(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTaskComments === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TaskCommentPeer::USERID, $this->id);

				$count = TaskCommentPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TaskCommentPeer::USERID, $this->id);

				if (!isset($this->lastTaskCommentCriteria) || !$this->lastTaskCommentCriteria->equals($criteria)) {
					$count = TaskCommentPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTaskComments);
				}
			} else {
				$count = count($this->collTaskComments);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a TaskComment object to this object
	 * through the TaskComment foreign key attribute.
	 *
	 * @param      TaskComment $l TaskComment
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTaskComment(TaskComment $l)
	{
		if ($this->collTaskComments === null) {
			$this->initTaskComments();
		}
		if (!in_array($l, $this->collTaskComments, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTaskComments, $l);
			$l->setUsr($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related TaskComments from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getTaskCommentsJoinTask($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTaskComments === null) {
			if ($this->isNew()) {
				$this->collTaskComments = array();
			} else {

				$criteria->add(TaskCommentPeer::USERID, $this->id);

				$this->collTaskComments = TaskCommentPeer::doSelectJoinTask($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TaskCommentPeer::USERID, $this->id);

			if (!isset($this->lastTaskCommentCriteria) || !$this->lastTaskCommentCriteria->equals($criteria)) {
				$this->collTaskComments = TaskCommentPeer::doSelectJoinTask($criteria, $con, $join_behavior);
			}
		}
		$this->lastTaskCommentCriteria = $criteria;

		return $this->collTaskComments;
	}

	/**
	 * Clears out the collPostComments collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPostComments()
	 */
	public function clearPostComments()
	{
		$this->collPostComments = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPostComments collection (array).
	 *
	 * By default this just sets the collPostComments collection to an empty array (like clearcollPostComments());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPostComments()
	{
		$this->collPostComments = array();
	}

	/**
	 * Gets an array of PostComment objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related PostComments from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array PostComment[]
	 * @throws     PropelException
	 */
	public function getPostComments($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPostComments === null) {
			if ($this->isNew()) {
			   $this->collPostComments = array();
			} else {

				$criteria->add(PostCommentPeer::USERID, $this->id);

				PostCommentPeer::addSelectColumns($criteria);
				$this->collPostComments = PostCommentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PostCommentPeer::USERID, $this->id);

				PostCommentPeer::addSelectColumns($criteria);
				if (!isset($this->lastPostCommentCriteria) || !$this->lastPostCommentCriteria->equals($criteria)) {
					$this->collPostComments = PostCommentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPostCommentCriteria = $criteria;
		return $this->collPostComments;
	}

	/**
	 * Returns the number of related PostComment objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related PostComment objects.
	 * @throws     PropelException
	 */
	public function countPostComments(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPostComments === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PostCommentPeer::USERID, $this->id);

				$count = PostCommentPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PostCommentPeer::USERID, $this->id);

				if (!isset($this->lastPostCommentCriteria) || !$this->lastPostCommentCriteria->equals($criteria)) {
					$count = PostCommentPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPostComments);
				}
			} else {
				$count = count($this->collPostComments);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a PostComment object to this object
	 * through the PostComment foreign key attribute.
	 *
	 * @param      PostComment $l PostComment
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPostComment(PostComment $l)
	{
		if ($this->collPostComments === null) {
			$this->initPostComments();
		}
		if (!in_array($l, $this->collPostComments, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPostComments, $l);
			$l->setUsr($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usr is new, it will return
	 * an empty collection; or if this Usr has previously
	 * been saved, it will retrieve related PostComments from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usr.
	 */
	public function getPostCommentsJoinPost($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPostComments === null) {
			if ($this->isNew()) {
				$this->collPostComments = array();
			} else {

				$criteria->add(PostCommentPeer::USERID, $this->id);

				$this->collPostComments = PostCommentPeer::doSelectJoinPost($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PostCommentPeer::USERID, $this->id);

			if (!isset($this->lastPostCommentCriteria) || !$this->lastPostCommentCriteria->equals($criteria)) {
				$this->collPostComments = PostCommentPeer::doSelectJoinPost($criteria, $con, $join_behavior);
			}
		}
		$this->lastPostCommentCriteria = $criteria;

		return $this->collPostComments;
	}

	/**
	 * Clears out the collRanks collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addRanks()
	 */
	public function clearRanks()
	{
		$this->collRanks = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collRanks collection (array).
	 *
	 * By default this just sets the collRanks collection to an empty array (like clearcollRanks());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initRanks()
	{
		$this->collRanks = array();
	}

	/**
	 * Gets an array of Rank objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related Ranks from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Rank[]
	 * @throws     PropelException
	 */
	public function getRanks($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRanks === null) {
			if ($this->isNew()) {
			   $this->collRanks = array();
			} else {

				$criteria->add(RankPeer::USERID, $this->id);

				RankPeer::addSelectColumns($criteria);
				$this->collRanks = RankPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(RankPeer::USERID, $this->id);

				RankPeer::addSelectColumns($criteria);
				if (!isset($this->lastRankCriteria) || !$this->lastRankCriteria->equals($criteria)) {
					$this->collRanks = RankPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastRankCriteria = $criteria;
		return $this->collRanks;
	}

	/**
	 * Returns the number of related Rank objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Rank objects.
	 * @throws     PropelException
	 */
	public function countRanks(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collRanks === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(RankPeer::USERID, $this->id);

				$count = RankPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(RankPeer::USERID, $this->id);

				if (!isset($this->lastRankCriteria) || !$this->lastRankCriteria->equals($criteria)) {
					$count = RankPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collRanks);
				}
			} else {
				$count = count($this->collRanks);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Rank object to this object
	 * through the Rank foreign key attribute.
	 *
	 * @param      Rank $l Rank
	 * @return     void
	 * @throws     PropelException
	 */
	public function addRank(Rank $l)
	{
		if ($this->collRanks === null) {
			$this->initRanks();
		}
		if (!in_array($l, $this->collRanks, true)) { // only add it if the **same** object is not already associated
			array_push($this->collRanks, $l);
			$l->setUsr($this);
		}
	}

	/**
	 * Clears out the collStatmsgs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStatmsgs()
	 */
	public function clearStatmsgs()
	{
		$this->collStatmsgs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStatmsgs collection (array).
	 *
	 * By default this just sets the collStatmsgs collection to an empty array (like clearcollStatmsgs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStatmsgs()
	{
		$this->collStatmsgs = array();
	}

	/**
	 * Gets an array of Statmsg objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related Statmsgs from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Statmsg[]
	 * @throws     PropelException
	 */
	public function getStatmsgs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStatmsgs === null) {
			if ($this->isNew()) {
			   $this->collStatmsgs = array();
			} else {

				$criteria->add(StatmsgPeer::USERID, $this->id);

				StatmsgPeer::addSelectColumns($criteria);
				$this->collStatmsgs = StatmsgPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StatmsgPeer::USERID, $this->id);

				StatmsgPeer::addSelectColumns($criteria);
				if (!isset($this->lastStatmsgCriteria) || !$this->lastStatmsgCriteria->equals($criteria)) {
					$this->collStatmsgs = StatmsgPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStatmsgCriteria = $criteria;
		return $this->collStatmsgs;
	}

	/**
	 * Returns the number of related Statmsg objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Statmsg objects.
	 * @throws     PropelException
	 */
	public function countStatmsgs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStatmsgs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StatmsgPeer::USERID, $this->id);

				$count = StatmsgPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StatmsgPeer::USERID, $this->id);

				if (!isset($this->lastStatmsgCriteria) || !$this->lastStatmsgCriteria->equals($criteria)) {
					$count = StatmsgPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collStatmsgs);
				}
			} else {
				$count = count($this->collStatmsgs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Statmsg object to this object
	 * through the Statmsg foreign key attribute.
	 *
	 * @param      Statmsg $l Statmsg
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStatmsg(Statmsg $l)
	{
		if ($this->collStatmsgs === null) {
			$this->initStatmsgs();
		}
		if (!in_array($l, $this->collStatmsgs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStatmsgs, $l);
			$l->setUsr($this);
		}
	}

	/**
	 * Clears out the collUsrCommentsRelatedByUserid collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addUsrCommentsRelatedByUserid()
	 */
	public function clearUsrCommentsRelatedByUserid()
	{
		$this->collUsrCommentsRelatedByUserid = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collUsrCommentsRelatedByUserid collection (array).
	 *
	 * By default this just sets the collUsrCommentsRelatedByUserid collection to an empty array (like clearcollUsrCommentsRelatedByUserid());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initUsrCommentsRelatedByUserid()
	{
		$this->collUsrCommentsRelatedByUserid = array();
	}

	/**
	 * Gets an array of UsrComment objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related UsrCommentsRelatedByUserid from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array UsrComment[]
	 * @throws     PropelException
	 */
	public function getUsrCommentsRelatedByUserid($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUsrCommentsRelatedByUserid === null) {
			if ($this->isNew()) {
			   $this->collUsrCommentsRelatedByUserid = array();
			} else {

				$criteria->add(UsrCommentPeer::USERID, $this->id);

				UsrCommentPeer::addSelectColumns($criteria);
				$this->collUsrCommentsRelatedByUserid = UsrCommentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(UsrCommentPeer::USERID, $this->id);

				UsrCommentPeer::addSelectColumns($criteria);
				if (!isset($this->lastUsrCommentRelatedByUseridCriteria) || !$this->lastUsrCommentRelatedByUseridCriteria->equals($criteria)) {
					$this->collUsrCommentsRelatedByUserid = UsrCommentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastUsrCommentRelatedByUseridCriteria = $criteria;
		return $this->collUsrCommentsRelatedByUserid;
	}

	/**
	 * Returns the number of related UsrComment objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related UsrComment objects.
	 * @throws     PropelException
	 */
	public function countUsrCommentsRelatedByUserid(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collUsrCommentsRelatedByUserid === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(UsrCommentPeer::USERID, $this->id);

				$count = UsrCommentPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(UsrCommentPeer::USERID, $this->id);

				if (!isset($this->lastUsrCommentRelatedByUseridCriteria) || !$this->lastUsrCommentRelatedByUseridCriteria->equals($criteria)) {
					$count = UsrCommentPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collUsrCommentsRelatedByUserid);
				}
			} else {
				$count = count($this->collUsrCommentsRelatedByUserid);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a UsrComment object to this object
	 * through the UsrComment foreign key attribute.
	 *
	 * @param      UsrComment $l UsrComment
	 * @return     void
	 * @throws     PropelException
	 */
	public function addUsrCommentRelatedByUserid(UsrComment $l)
	{
		if ($this->collUsrCommentsRelatedByUserid === null) {
			$this->initUsrCommentsRelatedByUserid();
		}
		if (!in_array($l, $this->collUsrCommentsRelatedByUserid, true)) { // only add it if the **same** object is not already associated
			array_push($this->collUsrCommentsRelatedByUserid, $l);
			$l->setUsrRelatedByUserid($this);
		}
	}

	/**
	 * Clears out the collUsrCommentsRelatedByUser2 collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addUsrCommentsRelatedByUser2()
	 */
	public function clearUsrCommentsRelatedByUser2()
	{
		$this->collUsrCommentsRelatedByUser2 = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collUsrCommentsRelatedByUser2 collection (array).
	 *
	 * By default this just sets the collUsrCommentsRelatedByUser2 collection to an empty array (like clearcollUsrCommentsRelatedByUser2());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initUsrCommentsRelatedByUser2()
	{
		$this->collUsrCommentsRelatedByUser2 = array();
	}

	/**
	 * Gets an array of UsrComment objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usr has previously been saved, it will retrieve
	 * related UsrCommentsRelatedByUser2 from storage. If this Usr is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array UsrComment[]
	 * @throws     PropelException
	 */
	public function getUsrCommentsRelatedByUser2($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collUsrCommentsRelatedByUser2 === null) {
			if ($this->isNew()) {
			   $this->collUsrCommentsRelatedByUser2 = array();
			} else {

				$criteria->add(UsrCommentPeer::USER2, $this->id);

				UsrCommentPeer::addSelectColumns($criteria);
				$this->collUsrCommentsRelatedByUser2 = UsrCommentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(UsrCommentPeer::USER2, $this->id);

				UsrCommentPeer::addSelectColumns($criteria);
				if (!isset($this->lastUsrCommentRelatedByUser2Criteria) || !$this->lastUsrCommentRelatedByUser2Criteria->equals($criteria)) {
					$this->collUsrCommentsRelatedByUser2 = UsrCommentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastUsrCommentRelatedByUser2Criteria = $criteria;
		return $this->collUsrCommentsRelatedByUser2;
	}

	/**
	 * Returns the number of related UsrComment objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related UsrComment objects.
	 * @throws     PropelException
	 */
	public function countUsrCommentsRelatedByUser2(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsrPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collUsrCommentsRelatedByUser2 === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(UsrCommentPeer::USER2, $this->id);

				$count = UsrCommentPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(UsrCommentPeer::USER2, $this->id);

				if (!isset($this->lastUsrCommentRelatedByUser2Criteria) || !$this->lastUsrCommentRelatedByUser2Criteria->equals($criteria)) {
					$count = UsrCommentPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collUsrCommentsRelatedByUser2);
				}
			} else {
				$count = count($this->collUsrCommentsRelatedByUser2);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a UsrComment object to this object
	 * through the UsrComment foreign key attribute.
	 *
	 * @param      UsrComment $l UsrComment
	 * @return     void
	 * @throws     PropelException
	 */
	public function addUsrCommentRelatedByUser2(UsrComment $l)
	{
		if ($this->collUsrCommentsRelatedByUser2 === null) {
			$this->initUsrCommentsRelatedByUser2();
		}
		if (!in_array($l, $this->collUsrCommentsRelatedByUser2, true)) { // only add it if the **same** object is not already associated
			array_push($this->collUsrCommentsRelatedByUser2, $l);
			$l->setUsrRelatedByUser2($this);
		}
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collBlogs) {
				foreach ((array) $this->collBlogs as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collBlogComments) {
				foreach ((array) $this->collBlogComments as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPermissions) {
				foreach ((array) $this->collPermissions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPicturesRelatedByUserid) {
				foreach ((array) $this->collPicturesRelatedByUserid as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPicturesRelatedByOwner) {
				foreach ((array) $this->collPicturesRelatedByOwner as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPictureComments) {
				foreach ((array) $this->collPictureComments as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collTests) {
				foreach ((array) $this->collTests as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPosts) {
				foreach ((array) $this->collPosts as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collTasksRelatedByUserid) {
				foreach ((array) $this->collTasksRelatedByUserid as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collTasksRelatedByAuthorid) {
				foreach ((array) $this->collTasksRelatedByAuthorid as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collTaskComments) {
				foreach ((array) $this->collTaskComments as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPostComments) {
				foreach ((array) $this->collPostComments as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collRanks) {
				foreach ((array) $this->collRanks as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStatmsgs) {
				foreach ((array) $this->collStatmsgs as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collUsrCommentsRelatedByUserid) {
				foreach ((array) $this->collUsrCommentsRelatedByUserid as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collUsrCommentsRelatedByUser2) {
				foreach ((array) $this->collUsrCommentsRelatedByUser2 as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collBlogs = null;
		$this->collBlogComments = null;
		$this->collPermissions = null;
		$this->collPicturesRelatedByUserid = null;
		$this->collPicturesRelatedByOwner = null;
		$this->collPictureComments = null;
		$this->collTests = null;
		$this->collPosts = null;
		$this->collTasksRelatedByUserid = null;
		$this->collTasksRelatedByAuthorid = null;
		$this->collTaskComments = null;
		$this->collPostComments = null;
		$this->collRanks = null;
		$this->collStatmsgs = null;
		$this->collUsrCommentsRelatedByUserid = null;
		$this->collUsrCommentsRelatedByUser2 = null;
			$this->aSysPic = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseUsr:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseUsr::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseUsr
